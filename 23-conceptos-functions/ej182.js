const animals = ["perro", "gato", "delfin", "elefante"];

// Utilizar el método map de los array para crear una colección con 
// todos los items de la colección animales en mayúscula.
// Utilizar una arrow function como callback del map.

animals.map((animal) => console.log(animal.toUpperCase()));
