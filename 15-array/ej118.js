const grades = [10, 3, 5, 4, 5, 6, 7, 8, 9, 4,3, 2];

// Sumar los valores del primer, cuarta, quinta y última entrega

const result = grades[0] + grades[3] + grades[4] + grades[grades.length -1];
console.log(result);

